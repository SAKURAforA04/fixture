@model IEnumerable<Fixture02.Models.Workcell>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/FixtureMaster.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WorkcellName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WorkcellName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WorkcellID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WorkcellID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WorkcellID })
        </td>
    </tr>
}

</table>
